---
import IconBrandFacebook from '@/assets/icons/IconBrandFacebook.svg';
import IconBrandInstagram from '@/assets/icons/IconBrandInstagram.svg';
import IconBrandWhatsapp from '@/assets/icons/IconBrandWhatsapp.svg';
import IconMail from '@/assets/icons/IconMail.svg';
import LightBoxImage from '@/assets/images/LightBox.png';
import Question from '@/components/Question.astro';
import { QUESTIONS } from '@/consts/questions';
import { SOCIAL_MEDIAS } from '@/consts/socialMedias';
import Layout from '@/layouts/Layout.astro';
import { Image } from 'astro:assets';
---

<Layout>
  <main class="pt-24 lg:pt-32">
    <section>
      <h1 class="mb-4 text-center text-2xl font-semibold lg:text-4xl">
        Hablemos de tu próximo proyecto
      </h1>
      <p class="mb-8 text-center text-lg text-balance lg:text-xl">
        Queremos que contactar con nosotros sea tan sencillo como tomarse una foto. Puedes
        escribirnos por WhatsApp, seguirnos en redes o enviarnos un correo con los detalles de tu
        proyecto. Elige tu canal favorito y comencemos a trabajar en tus imágenes.
      </p>
      <div class="gap-20 lg:grid lg:grid-cols-3">
        <Image
          src={LightBoxImage}
          alt="Placeholder"
          class="hidden self-end justify-self-end lg:block"
        />
        <div class="grid grid-cols-1 gap-2 font-heading max-lg:p-4">
          <div class="flex items-center gap-2 rounded-lg bg-midnight p-4 text-white">
            <IconBrandWhatsapp class="h-8 w-8" />
            <p>{SOCIAL_MEDIAS.WhatsApp.name}</p>
          </div>
          <div class="flex items-center gap-2 rounded-lg bg-midnight p-4 text-white">
            <IconBrandInstagram class="h-8 w-8" />
            <p>{SOCIAL_MEDIAS.Instagram.name}</p>
          </div>
          <div class="flex items-center gap-2 rounded-lg bg-midnight p-4 text-white">
            <IconBrandFacebook class="h-8 w-8" />
            <p>{SOCIAL_MEDIAS.Facebook.name}</p>
          </div>
          <div class="flex items-center gap-2 rounded-lg bg-midnight p-4 text-white">
            <IconMail class="h-8 w-8" />
            <p>{SOCIAL_MEDIAS.Email.name}</p>
          </div>
        </div>
        <Image
          class="hidden -scale-x-100 transform self-end justify-self-start lg:block"
          src={LightBoxImage}
          alt="Placeholder"
        />
      </div>
    </section>
    <section class="mx-auto py-20 lg:max-w-6xl">
      <h2 class="mb-10 text-center text-2xl font-semibold lg:text-4xl">Preguntas Frecuentes</h2>
      <div class="flex flex-col gap-4 px-4">
        {QUESTIONS.map((question) => <Question question={question} />)}
      </div>
    </section>
  </main>
</Layout>
